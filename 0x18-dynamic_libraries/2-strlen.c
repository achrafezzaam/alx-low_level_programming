#include <string.h>
/**
 * _strlen - return the length of a string
 * @s: the string to get the length of
 *
 * Description: take a string as a parameter
 * and return it's length using the string
 * library's strlen
 * Return: the length of the string
 */
int _strlen(char *s)
{
	return (strlen(s));
}
