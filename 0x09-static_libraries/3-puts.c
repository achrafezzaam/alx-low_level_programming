#include <stdio.h>
/**
 * _puts - print a string
 * @str: the string to print
 *
 * Description: take a pointer as parameter
 * and prints the variable it points to
 */
void _puts(char *str)
{
	puts(str);
}
